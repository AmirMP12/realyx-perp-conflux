{
  "version": 2,
  "buildCommand": "cd backend && npm ci && npm run build && cd ../frontend && npm ci && npm run build",
  "installCommand": "npm ci && cd backend && npm ci && cd ../frontend && npm ci",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/**/*.mjs": {
      "includeFiles": "backend/{dist,node_modules}/**",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/health",
      "destination": "/api"
    }
  ]
}