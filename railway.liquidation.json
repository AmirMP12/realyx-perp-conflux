{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.keeper"
  },
  "deploy": {
    "startCommand": "npx tsx scripts/liquidation-bot.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
